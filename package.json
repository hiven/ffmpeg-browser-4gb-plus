{
  "name": "my-ffmpeg-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "copy-ffmpeg": "mkdir -p public/ffmpeg && cp -R node_modules/@ffmpeg/ffmpeg/dist/umd/* public/ffmpeg/ && cp -R node_modules/@ffmpeg/core/dist/umd/* public/ffmpeg/ && cp node_modules/@ffmpeg/util/dist/umd/index.js public/ffmpeg/util.js",
    "prebuild": "npm run copy-ffmpeg",
    "build": "npm run prebuild"
  },
  "dependencies": {
    "@ffmpeg/ffmpeg": "0.12.6",
    "@ffmpeg/core":   "0.12.6",
    "@ffmpeg/util":   "0.12.2"
  }
}
